# SHA-256 hex hash of the channel-exporter passcode.
# Leave unset to hide the channel exporter entirely from a build.
# Generate: node -e "console.log(require('crypto').createHash('sha256').update('your-passcode').digest('hex'))"
VITE_CHANNEL_PASSCODE_HASH=
CHANNEL_EXPORT_PASSCODE_HASH=
